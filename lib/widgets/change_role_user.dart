import 'package:flutter/material.dart';

class ChangeRoleToUserScreen extends StatelessWidget {
  static const String rounterName = "change_role_user";
  const ChangeRoleToUserScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ChangeRoleToUserScreen'),
      ),
    );
  }
}
